# ecofr

## Changelog

### 2026-03-10
- Updated `index.html` page title to **Save the Planet - Prototype**.
- Replaced the previous game HTML with a simplified prototype/placeholder structure.
- Added starter (placeholder) JavaScript stubs for:
  - ending the game (`endGame(success)`)
  - fetching/updating a JSONBin-based leaderboard (`fetchLeaderboard()")

> Note: The leaderboard section still requires a real JSONBin `BIN_ID` and access key, and the prototype stubs are not yet integrated with the original game logic.